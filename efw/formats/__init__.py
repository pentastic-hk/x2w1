"""Output-format implementations. Each module builds a complete
python-docx Document for one --format value, reusing the shared
data-extraction pipeline (efw.excel_source / efw.extraction) and the
shared docx-building helpers (efw.docx_utils).
"""
