Now add a new output format. It's name should be "veri-summary-by-section", meaning "Verification Summary per Section", and be selectable via the CLI flag. The output should be a table in an A4 portrait. A sample table is provided. Create a new section for every section you've detected from the excel file. Note that every cell in the table should count the number of items by their verification status. The row "Critical" risk level should only be shown above "High" if such findings exist, hidden otherwise. The column "Partially Completed" should only be shown between columns "Completed" and "Incomplete" if such status exist, hidden otherwise. Follow all the fonts, font-sizes, font-colors, table-colors and other coloring schemes. You should re-use as much as possible the data-retrieval logic from the existing python script.
