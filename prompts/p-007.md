For verification status, the output values should only be ["Completed", "Partially Completed", "Incomplete", "Scheduled", "Accepted"]. The parsing should check if the cell string BEGINS WITH any of the above values case insensitive, and drop the remaining string from appearing in the word table. Warnings should be shown if none of the 5 values are matched.
