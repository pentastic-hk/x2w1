Great. Now write me a python script that does the conversion from excel to word.
First let me give you some context.
The excel file is a follow-up plan where one of its sheets contains a table that lists cybersecurity findings from general control review, vulnerability scanning, web penetration testing, api penetration testing, source code review, etc.
Our targeted output is a word table to be included in a formal report which details all the findings and their metadata.

Here's the procedural steps / requirements of the conversion.
1. Locate the sheet named "Follow-up Items" (which is usually the 3rd sheet).
2. Locate the top-left corner of the table. The table might not start at cell A1, but at cell B3 or B4 or any other location. The information you should rely on, is that the target table are the only cells that have borders.
3. The table has a layout as belows. Note that some columns are optional, and I'd wrap such column names with brackets. Note that some columns have uncertain column-names or need extra explanation, where I'd explain them inside C-like comments "/*" and "*/".
| | Finding | Affected | Risk Level | (Impact) | (Likelihood) | Risk Description | Recommended Safeguards | /* Vendor Response, may vary depending on project */ | /* starts with the string "Verification", the full string is sometimes named "Verification by SRAA Contractor", sometimes named "Verification by Pentastic on 29.08.2026", sometimes named "Verification by DPO" */ | /* optionally another column that starts with the string "Verification". Sometimes we'd perform verification multiple times. Our convention is that a column in the right is a later or new verification status. If multiple verification columns exist, we'll take the verification status for each finding of the last non-empty column. */ |
| /* all cells in this row are merged into one cell, usually named "General Control Review" or "Vulnerability Scanning" or "Penetration Testing" which highlights the following section */ |
| W1 | CORS: Arbitrary origin trusted | https://www.company.com/api/v1/admin | Medium | High | Medium | An HTML5 cross-origin resource sharing (CORS) policy controls whether and how content running on other domains ...\n\n(another paragraph)\n(another paragraph) | Rather than using a wildcard or programmatically verifying supplied origins, use a whitelist of trusted domains. ...\n\n(another paragraph)\n(another paragraph) | Fixed | Completed. Vendor added ... |
| W2 | Cookie without HTTPOnly flag set | POST /api/v2/admins/pwAuth/login | Low | Low | Medium | If the HttpOnly attribute is set on a cookie, then the cookie's value cannot be read or set by client-side JavaScript.\n\n(another paragraph) | Set the HttpOnly flag by including this attribute within ...\n\n(another paragraph)\n(another paragraph) | to be completed by Q1 2028 | Scheduled |
| Penetration Testing |
| MP1 | CORS: Arbitrary origin trusted | https://www.company.com/api/v1/admin | Medium | High | Medium | An HTML5 cross-origin resource sharing (CORS) policy controls whether and how content running on other domains ...\n\n(another paragraph)\n(another paragraph) | Rather than using a wildcard or programmatically verifying supplied origins, use a whitelist of trusted domains. ...\n\n(another paragraph)\n(another paragraph) | This is a false alarm. Actually, ... | Accepted |
| MP2 | Cookie without HTTPOnly flag set | POST /api/v2/admins/pwAuth/login | Low | Low | Medium | If the HttpOnly attribute is set on a cookie, then the cookie's value cannot be read or set by client-side JavaScript.\n\n(another paragraph) | Set the HttpOnly flag by including this attribute within ...\n\n(another paragraph)\n(another paragraph) | Fixed | Completed. |

Note that Risk Level should only have the values ["Critical", "High", "Medium", "Low", "OFI"] (case-insensitive), and the python script should warn the user if unexpected values occur.
Note that Impact should only have the values ["Critical", "High", "Medium", "Low", "Very Low"] (case-insensitive), and the python script should warn the user if unexpected values occur.
Note that Likelihood should only have the values ["High", "Medium", "Low", "Very Low"] (case-insensitive), and the python script should warn the user if unexpected values occur.
Note that the verification status should only BEGIN WITH ["Completed", "Partially completed", "Incomplete", "Scheduled", "Accepted"] (case-insensitive), and the python script should warn the user if unexpected values occur.

4. Extract the contents in the excel and convert them into many word tables with the following format:
| W1 | CORS: Arbitrary origin trusted |
| Risk Description | An HTML5 cors-origin resource sharing (CORS) policy ... |
| Risk Level | Medium |
| Impact/Likelihood | High/Medium |
| OWASP Top 10 | /* please leave this cell as an empty string */ |
| Affected Asset | https://www.company.com/api/v1/admin |
| Evidence for the finding | /* please leave this cell as an empty string */ |
| Recommended Safeguards | Rather than using a wildcard or programmatically verifying... |
| Evidence for the remedial actions | /* please leave this cell as an empty string */ |
| Rectification status as of dd MMM yyyy | Completed |

| W2 | Cookie without HTTPOnly flag set |
| Risk Description | If the HttpOnly attribute is set on ... |
| Risk Level | Low |
| Impact/Likelihood | Low/Medium |
| OWASP Top 10 | /* please leave this cell as an empty string */ |
| Affected Asset | POST /api/v2/admins/pwAuth/login |
| Evidence for the finding | /* please leave this cell as an empty string */ |
| Recommended Safeguards | Set the HttpOnly flag by including this attribute... |
| Evidence for the remedial actions | /* please leave this cell as an empty string */ |
| Rectification status as of dd MMM yyyy | Scheduled |

Separate each word table with 2 empty lines.

5. Output the word docx file with the resultant table.

Also give me the list of dependencies in a `requirements.txt` file in pip-supported format.
